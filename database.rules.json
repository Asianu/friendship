// These rules grant access to a node matching the authenticated
// user's ID from the Firebase auth token
{
  "rules": {
    "users": {
      "$uid": {
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid"
      }
    },
    "mentors": {
      ".read": "auth != null",
      ".write": "auth != null"
    },
    "activities": {
      ".read": true,
      ".write": false
    }
  }
}